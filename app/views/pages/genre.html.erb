
<%= render 'shared/sidebar' %>

<!--create helper if params don't match any Tv shows-->
<% if genre_found? %>
  <%=render 'genre_cards'%>
<% else %>
  <%= render 'shared/genre_not_found'%>
<% end %>
